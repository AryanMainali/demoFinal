# ML packages for AI-generated code detection.
# torch is installed CPU-only to keep the Docker image small (~500MB vs ~2GB).
# The extra-index-url is set in the Dockerfile pip install command.
torch==2.2.2
sentence-transformers>=3.0.0,<4.0.0
transformers>=4.41.0,<5.0.0
tokenizers>=0.19.0,<0.22.0
xgboost>=2.0.0,<3.0.0
scikit-learn>=1.4.0,<2.0.0
datasets>=2.18.0,<4.0.0
joblib>=1.3.0,<2.0.0
